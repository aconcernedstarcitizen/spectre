# This workflow will build a golang project and create releases
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Build and Release

on:
  push:
    branches: [ "main", "dev" ]

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'

    - name: Download dependencies
      run: go mod download

    - name: Test
      run: go test -v ./...

    - name: Build for macOS (amd64)
      run: |
        GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o specter .
        mkdir -p specter-macos-amd64
        mv specter specter-macos-amd64/
        cp config.yaml specter-macos-amd64/
        cp -r lang specter-macos-amd64/
        zip -r specter-macos-amd64.zip specter-macos-amd64

    - name: Build for macOS (arm64)
      run: |
        GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o specter .
        mkdir -p specter-macos-arm64
        mv specter specter-macos-arm64/
        cp config.yaml specter-macos-arm64/
        cp -r lang specter-macos-arm64/
        zip -r specter-macos-arm64.zip specter-macos-arm64

    - name: Build for Windows (amd64)
      run: |
        GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o specter.exe .
        mkdir -p specter-windows-amd64
        mv specter.exe specter-windows-amd64/
        cp config.yaml specter-windows-amd64/
        cp -r lang specter-windows-amd64/
        zip -r specter-windows-amd64.zip specter-windows-amd64

    - name: Build for Linux (amd64)
      run: |
        GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o specter .
        mkdir -p specter-linux-amd64
        mv specter specter-linux-amd64/
        cp config.yaml specter-linux-amd64/
        cp -r lang specter-linux-amd64/
        zip -r specter-linux-amd64.zip specter-linux-amd64

    - name: Generate release tag
      id: tag
      run: |
        BRANCH_NAME="${GITHUB_REF#refs/heads/}"
        if [ "$BRANCH_NAME" = "dev" ]; then
          echo "release_tag=dev-build-$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT
          echo "is_dev=true" >> $GITHUB_OUTPUT
        else
          echo "release_tag=build-$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT
          echo "is_dev=false" >> $GITHUB_OUTPUT
        fi

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.tag.outputs.release_tag }}
        name: ${{ steps.tag.outputs.is_dev == 'true' && '🚧 Dev Release' || 'Release' }} ${{ steps.tag.outputs.release_tag }}
        prerelease: ${{ steps.tag.outputs.is_dev == 'true' }}
        body: |
          ${{ steps.tag.outputs.is_dev == 'true' && '🚧 **DEVELOPMENT BUILD** - This is a pre-release build from the dev branch for testing new features.' || 'Automated release from the main branch.' }}

          Commit: ${{ github.sha }}

          **Download Instructions:**
          1. Download the ZIP file for your platform below
          2. Extract the ZIP file to a folder
          3. The folder contains:
             - `specter` (or `specter.exe` on Windows) - The executable
             - `config.yaml` - Configuration file
             - `lang/` - Language files (English + Russian)
          4. Edit `config.yaml` to set your preferences
          5. Run `specter` from the command line

          **Included packages:**
          - specter-macos-amd64.zip: macOS Intel package
          - specter-macos-arm64.zip: macOS Apple Silicon package
          - specter-windows-amd64.zip: Windows package
          - specter-linux-amd64.zip: Linux package

          **Key Features:**
          - ⚡ **Ultra-fast API-based checkout** - Completes purchase in under 1 second once item is in cart
          - 🌊 **Smart Multi-Wave Mode** - Start at ANY time, app automatically skips past waves and finds the next relevant one
          - 🕐 **Precise Time Synchronization** - Syncs with Amazon's time server for accurate wave timing (CIG infrastructure runs on AWS)
          - 📅 **User-Friendly Time Format** - Just paste times like "2025-01-15 16:00 UTC" from CIG announcements
          - 🎯 **Smart Wave Detection** - Automatically determines which wave to process based on current time
          - 💤 **Dormant State Management** - Stays dormant between waves, no need to restart the app
          - 🔄 **Intelligent Retry System** - Smart backoff for rate limits, specific delays for different error types
          - 🔐 **reCAPTCHA v3 Enterprise** - Generates fresh tokens for each cart addition attempt
          - 💳 **Automatic Store Credit Application** - No manual steps needed
          - 🌍 **Multi-language Support** - English & Russian with automatic system locale detection

          **How It Works:**
          - **Time-Based Wave Selection:** App compares current time against all wave end times to determine starting point
          - **Pre-Wave Polling:** Starts checking product page 2 minutes before each wave (configurable)
          - **Post-Wave Timeout:** Continues trying for 5 minutes after wave starts (configurable)
          - **Automatic Transitions:** Moves to next wave on failure, exits gracefully on success
          - **Past Wave Skipping:** If you start late, app automatically skips waves that already ended
          - **All Waves Ended Detection:** Informs user if all waves have passed and exits gracefully

          **Important Notes:**
          - ✅ **Works ONLY with store credit** - Cannot process credit card or PayPal payments
          - ✅ **Single ship purchases only** - Designed for buying one ship at a time
          - 📋 **Setup required:** Configure sale_windows in config.yaml with wave times before running
        files: |
          specter-macos-amd64.zip
          specter-macos-arm64.zip
          specter-windows-amd64.zip
          specter-linux-amd64.zip
